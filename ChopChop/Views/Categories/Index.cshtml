@*@model IEnumerable<CategoryViewModel>

@{
    ViewData["Title"] = "Categories";
}

<h2>Browse by Category</h2>
<ul>
    @foreach (var category in Model)
    {
        <li>
            <a asp-controller="Recipes" asp-action="Search" asp-route-categoryId="@category.Id">
                @category.Name
            </a>
        </li>
    }
</ul>
*@