@*@model RecipeSearchViewModel

@{
    ViewData["Title"] = "Search Recipes";
}

<h2>Search Results</h2>

<form asp-action="Search" method="get">
    <input type="text" name="query" value="@Model.Query" placeholder="Search..." class="form-control" />
    <button type="submit" class="btn btn-secondary mt-2">Search</button>
</form>

@if (Model.Results.Any())
{
    <div class="row mt-3">
        @foreach (var recipe in Model.Results)
        {
            <partial name="_RecipeCardPartial" model="recipe" />
        }
    </div>
}
else
{
    <p>No results found.</p>
}
*@